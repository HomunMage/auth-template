# .env.example
#
# SECURITY: All OAuth secrets are kept server-side (backend only).
# Frontend only needs client IDs for initiating OAuth flows.
# Token exchange happens through backend to protect secrets.

# --------------------------------------------------
# Authentik OAuth (public client - no secret needed)
# https://goauthentik.io/
# --------------------------------------------------
AUTHENTIK_URL="https://your-authentik-instance.com"
AUTHENTIK_CLIENT_ID="your-authentik-client-id"

# --------------------------------------------------
# Google OAuth
# Get credentials from https://console.cloud.google.com/apis/credentials
# --------------------------------------------------
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
# BACKEND ONLY - Never expose this to frontend!
GOOGLE_CLIENT_SECRET="your-google-client-secret"
